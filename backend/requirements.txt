# Flask and Web Framework
Flask>=2.3.0
Flask-CORS>=4.0.0
Flask-SQLAlchemy>=3.0.0
Flask-Migrate>=4.0.0

# Authentication
PyJWT>=2.8.0

# Environment and Configuration
python-dotenv>=1.0.0
Werkzeug>=2.3.0

# Data Processing
numpy>=1.24.0
openpyxl>=3.1.5

# Computer Vision (for boulder detection)
opencv-python>=4.8.0
Pillow>=10.0.0

# Machine Learning and AI
torch>=2.3.0
torchvision>=0.19.0
ultralytics>=8.3.0
timm>=0.9.0
grad-cam>=1.5.5
transformers>=4.35.0

# Note: ML packages (torch, transformers, etc.) may have compatibility issues 
# with Python 3.13. Install them separately if needed for boulder detection. 
